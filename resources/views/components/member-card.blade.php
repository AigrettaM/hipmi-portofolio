<div class="flex">
    <img src="{{ $image }}" class="rounded-2" width="200" alt="{{ $name }}">
    <div class="text-center mb-3 mt-3">
        <h4 class="fw-bold text-primary">{{ $name }}</h4>
        <p class="text-dark">{{ $position }}</p>
    </div>
</div>
