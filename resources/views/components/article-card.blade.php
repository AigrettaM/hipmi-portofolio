<div class="carousel-card card">
    <img src="{{ $image }}" class="card-img-top mt-3" alt="{{ $title }}">
    <div class="card-body text-center mb-3 mt-3">
        <h6 class="card-title">{{ $title }}</h6>
        <a href="{{ $link }}" class="btn btn-custom px-4 py-2 rounded-pill fw-bold">See more</a>
    </div>
</div>